import { Typography } from '@mui/material';

export const PokedexHeader = () => {
  return (
    <Typography variant='h4' component='h1' sx={{ mb: 5, fontWeight: 'bold' }}>
      Pokédex
    </Typography>
  );
};

